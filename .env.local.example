# Base URL of the Boundless Missions bot API (the in-process FastAPI server).
# The Next.js BFF route handlers proxy to this server-side; it is never exposed
# to the browser. Use the public VPS URL in production, localhost in dev.
BOT_API_URL=http://localhost:5022
